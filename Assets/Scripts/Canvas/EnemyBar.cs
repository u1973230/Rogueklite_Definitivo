using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;




public class EnemyBar : MonoBehaviour
{
/*
    private enum BarType{
    health,
    energy,
    mana
}
    
    [SerializeField] BarType m_type = BarType.health;
    //[SerializeField] string scriptName = "";
    //


    Image bar;



    float value, maxValue;


    private void Start() {

        bar=GetComponent<Image>();
        if(m_type == BarType.health){
            maxValue = BossManager.sharedInstance.boss_max_health;
        }

        value = maxValue;
        




    }

    void Update()
    {
        if(m_type == BarType.health){
            value = BossManager.sharedInstance.boss_health;
        }

        bar.fillAmount = value/maxValue;

        

    }*/
}
